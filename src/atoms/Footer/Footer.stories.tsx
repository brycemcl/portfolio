import Footer from '.'

export default {
  title: 'atoms/Footer',
  component: Footer,
}
const Template = (args) => <Footer {...args} />

export const standard = Template.bind({})
standard.args = {}
export {}
